<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Ajouter un film et une recette - Ciné Délices</title>
    <meta
      name="description"
      content="Ciné Délices — Ajoutez votre film et votre recette inspirée"
    />

    <!-- Ressources globales -->
    <%- include("partials/head-resources") %>

    <!-- Styles -->
    <link rel="stylesheet" href="/css/reset.css" />
    <link rel="stylesheet" href="/css/base.css" />
    <link rel="stylesheet" href="/css/add-recipes-movies.css" />
  </head>
  <body>
    <!-- HEADER -->
    <%- include("partials/header") %>

    <!-- MAIN -->
    <main class="add-recipe-main">
      <!-- Message de confirmation/erreur -->
      <% if (typeof success !== "undefined" && success && typeof successMessage
      !== "undefined" && successMessage) { %>
      <div class="alert alert-success">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><circle cx="12" cy="12" r="10"/><path d="m9 12 2 2 4-4"/></svg>
        <p><%= successMessage %></p>
      </div>
      <% } %> <% if (typeof error !== "undefined" && error && typeof
      errorMessage !== "undefined" && errorMessage) { %>
      <div class="alert alert-error">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><circle cx="12" cy="12" r="10"/><path d="m15 9-6 6"/><path d="m9 9 6 6"/></svg>
        <p><%= errorMessage %></p>
      </div>
      <% } %> <% if (typeof duplicateWarning !== "undefined" &&
      duplicateWarning) { %>
      <div class="alert alert-info">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4"/><path d="M12 8h.01"/></svg>
        <p>
          Ce film existe déjà dans notre base de données. Vous pouvez continuer
          à ajouter votre recette.
        </p>
      </div>
      <% } %>

      <!-- BANNIÈRE HERO -->
      <section class="hero-banner">
        <div class="hero-content"></div>
      </section>

      <!-- SECTION 1: AJOUT DU FILM -->
      <section class="add-film-section">
        <div class="section-header-film">
          <h2>1. J'ajoute mon film ou ma série</h2>
        </div>

        <!-- Formulaire d'ajout de film (pour prévisualisation seulement) -->
        <form id="film-preview-form" method="post" onsubmit="return false;">
          <div class="film-form-container">
            <div class="overlay"></div>
            <div class="film-inputs-row">
              <!-- Wrapper pour l'autocomplétion avec position relative -->
              <div class="film-input-group film-input-group--title">
                <label for="film-name" class="film-input-label">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><rect width="18" height="18" x="3" y="3" rx="2"/><path d="M7 3v18"/><path d="M3 7.5h4"/><path d="M3 12h18"/><path d="M3 16.5h4"/><path d="M17 3v18"/><path d="M17 7.5h4"/><path d="M17 16.5h4"/></svg>
                  <span>Nom du film ou de la série</span>
                </label>
                <div class="film-name-input-wrapper">
                  <input type="text" name="title" id="film-name"
                  class="form-input film-input-enhanced" spellcheck="true"
                  lang="fr en" autocomplete="off" <% if (typeof newMovie ===
                  "undefined" || !newMovie || newMovie.length === 0) { %>
                  placeholder="Ex: Retour vers le futur, Breaking Bad..." <% }
                  else { %> placeholder="<%= newMovie.title %>" <% } %> value=""
                  />
                  <!-- Dropdown pour les résultats de recherche -->
                  <div
                    id="film-search-results"
                    class="film-search-results"
                    aria-live="polite"
                  ></div>
                </div>
              </div>

              <div class="film-input-group film-input-group--year">
                <label for="film-year" class="film-input-label">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
                  <span>Année</span>
                </label>
                <input type="number" name="year" id="film-year"
                class="form-input film-input-enhanced" min="1888" max="<%= new
                Date().getFullYear() + 5 %>" <% if (typeof newMovie ===
                "undefined" || !newMovie || newMovie.length === 0) { %>
                placeholder="Ex: 1985" <% } else { %> placeholder="<%=
                newMovie.year %>" <% } %> value="" />
              </div>

              <div class="film-input-group film-input-group--genre">
                <label for="film-genre" class="film-input-label">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><path d="M9 5H2v7l6.29 6.29c.94.94 2.48.94 3.42 0l3.58-3.58c.94-.94.94-2.48 0-3.42L9 5Z"/><path d="M6 9.01V9"/><path d="m15 5 6.3 6.3a2.4 2.4 0 0 1 0 3.4L17 19"/></svg>
                  <span>Genre</span>
                </label>
                <select
                  name="genre"
                  id="film-genre"
                  class="form-select film-input-enhanced"
                >
                  <% if (typeof newMovie === "undefined" || !newMovie ||
                  newMovie.length === 0) { %>
                  <option value="">Genre</option>
                  <% } else { %>
                  <option value=""><%= newMovie.genre %></option>
                  <% } %>

                  <option value="action">Action</option>
                  <option value="animation">Animation</option>
                  <option value="aventure">Aventure</option>
                  <option value="comédie">Comédie</option>
                  <option value="crime">Crime</option>
                  <option value="documentaire">Documentaire</option>
                  <option value="drame">Drame</option>
                  <option value="familial">Familial</option>
                  <option value="fantastique">Fantastique</option>
                  <option value="guerre">Guerre</option>
                  <option value="histoire">Histoire</option>
                  <option value="horreur">Horreur</option>
                  <option value="musique">Musique</option>
                  <option value="mystère">Mystère</option>
                  <option value="romance">Romance</option>
                  <option value="science-fiction">Science-Fiction</option>
                  <option value="thriller">Thriller</option>
                  <option value="téléfilm">Téléfilm</option>
                  <option value="western">Western</option>
                </select>
              </div>
            </div>

            <!-- Synopsis du film : visible uniquement pour les admins -->
            <% if (typeof role !== "undefined" && role === "admin") { %>
            <div class="film-synopsis-row">
              <div class="film-input-group film-input-group--synopsis">
                <label for="film-synopsis" class="film-input-label">
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14 2 14 8 20 8"/><line x1="16" x2="8" y1="13" y2="13"/><line x1="16" x2="8" y1="17" y2="17"/><line x1="10" x2="8" y1="9" y2="9"/></svg>
                  <span>Synopsis</span>
                </label>
                <textarea
                  name="synopsis"
                  id="film-synopsis"
                  class="form-textarea film-input-enhanced"
                  rows="2"
                  spellcheck="true"
                  lang="fr"
                  maxlength="500"
                  placeholder="Résumé court du film ou de la série..."
                ><% if (typeof newMovie !== "undefined" && newMovie && newMovie.synopsis) { %><%= newMovie.synopsis %><% } %></textarea>
              </div>
            </div>
            <% } %>
          </div>

          <div class="form-footer">
            <p>
              Une fois votre film ou série renseigné, vous pourrez passer à
              l'ajout de votre recette inspirée du grand écran.
            </p>
            <% if (typeof newMovie === "undefined" || !newMovie ||
            newMovie.length === 0) { %>
            <button class="btn btn--red form-footer-btn">
              Je passe à la recette
            </button>
            <% } %>
          </div>
        </form>
      </section>

      <!-- SECTION 2: AJOUT DE LA RECETTE -->
      <section class="add-recipe-section">
        <div class="section-header-recipe">
          <h2>2. Je veux ajouter une recette</h2>
        </div>

        <!-- FORMULAIRE UNIFIÉ FILM + RECETTE-->
        <!-- AJOUT enctype="multipart/form-data"-->
        <form
          id="unified-form"
          action="/add-recipes-movies/movie-and-recipe"
          method="post"
          enctype="multipart/form-data"
        >
          <!-- STOCKAGE ID DU FILM (sera rempli par l'autocomplétion ou laisser vide pour nouveau film) -->
          <input type="hidden" name="filmId" id="filmId-hidden" value="<% if
          (typeof newMovie !== "undefined" && newMovie && newMovie.id) { %><%=
          newMovie.id %><% } %>" />

          <!-- Champs film (utilisés si filmId vide, pour créer un nouveau film) -->
          <input type="hidden" name="title" id="film-title-hidden" value="" />
          <input type="hidden" name="year" id="film-year-hidden" value="" />
          <input type="hidden" name="genre" id="film-genre-hidden" value="" />
          <input type="hidden" name="synopsis" id="film-synopsis-hidden" value="" />

          <!-- Champs TMDB pour validation intelligente -->
          <input type="hidden" name="tmdbId" id="tmdbId-hidden" value="" />
          <input type="hidden" name="titleFR" id="titleFR-hidden" value="" />
          <input type="hidden" name="tmdbYear" id="tmdbYear-hidden" value="" />
          <input
            type="hidden"
            name="tmdbGenre"
            id="tmdbGenre-hidden"
            value=""
          />

          <!-- Container 3 colonnes -->
          <div class="recipe-form-container">
            <!-- Colonne gauche: Film sélectionné -->
            <div class="recipe-column recipe-column-left">
              <div class="overlay"></div>
              <h3>Mon film sélectionné</h3>
              <div class="film-selected-image">
                <% let hasMovieWithImage = false; if (typeof newMovie !==
                "undefined" && newMovie && (newMovie.cardPath ||
                newMovie.picture) && typeof (newMovie.cardPath ||
                newMovie.picture) === "string" && (newMovie.cardPath ||
                newMovie.picture).trim() !== "") { hasMovieWithImage = true; }
                %> <% if (hasMovieWithImage) { %>
                <img
                  src="<%= newMovie.cardPath || newMovie.picture %>"
                  alt="Image du film ajouté par un admin Ciné Délices"
                />
                <% } else { %>
                <img
                  src="/images/image-default-movie.jpg"
                  alt="Image de film par defaut"
                />
                <% } %>
              </div>
              <% if (!hasMovieWithImage) { %>
              <p class="film-image-note">
                * L'image de votre film sera intégrée prochainement par Ciné
                Délices
              </p>
              <% } %>
              <div class="film-info-box">
                <h4>Les infos du film</h4>

                <% if (typeof newMovie === "undefined" || !newMovie ||
                newMovie.length === 0) { %>
                <ul>
                  <li id="display-film-name">titre</li>
                  <li id="display-film-genre">genre</li>
                  <li id="display-film-year">année</li>
                </ul>
                <p id="display-film-synopsis" class="film-info-synopsis">synopsis</p>
                <% } else { %>
                <ul>
                  <li id="display-film-name"><%= newMovie.title %></li>
                  <li id="display-film-genre"><%= newMovie.genre %></li>
                  <li id="display-film-year"><%= newMovie.year %></li>
                </ul>
                <p id="display-film-synopsis" class="film-info-synopsis"><%= newMovie.synopsis || 'synopsis' %></p>
                <% } %>
              </div>
            </div>

            <!-- Colonne centrale: Ma recette -->
            <div class="recipe-column recipe-column-center">
              <div class="overlay"></div>
              <h3>Ma recette</h3>
              <p class="recipe-description">
                Partagez votre recette inspirée du film ou de la série que vous
                avez sélectionné. Décrivez-la clairement pour que la communauté
                Ciné Délices puisse la cuisiner à son tour.
              </p>
              <input
                type="text"
                name="name"
                id="recipe-name"
                class="form-input"
                placeholder="Le nom de la recette"
                value=""
                spellcheck="true"
                lang="fr"
              />
              <input
                type="number"
                name="servings"
                id="recipe-servings"
                class="form-input"
                min="1"
                step="1"
                placeholder="Pour combien de personnes"
                value=""
              />
              <input
                type="number"
                name="time"
                id="recipe-time"
                class="form-input"
                min="1"
                step="1"
                placeholder="Temps de préparation en minutes (ex: 70)"
                value=""
              />
              <select name="category" id="category" class="form-select">
                <option value="">catégorie</option>
                <option value="apéritif">apéritif</option>
                <option value="entrée">entrée</option>
                <option value="plat">plat</option>
                <option value="dessert">dessert</option>
                <option value="boisson">boisson</option>
                <option value="autres">autres</option>
              </select>
              <select name="difficulty" id="difficulty" class="form-select">
                <option value="">Difficulté</option>
                <option value="Facile">Facile</option>
                <option value="Moyenne">Moyenne</option>
                <option value="Difficile">Difficile</option>
              </select>
            </div>

            <!-- Colonne droite: Image recette et contexte -->
            <div class="recipe-column recipe-column-right">
              <div class="overlay"></div>
              <h3>Image de la recette</h3>
              <div class="recipe-image-upload">
                <img
                  id="recipeImagePreview"
                  src="/images/image-default-movie.jpg"
                  alt="Image par défaut Ciné Délices"
                />
                <!-- Input file caché pour l'upload d'image -->
                <input
                  type="file"
                  name="recipeImage"
                  id="recipeImageInput"
                  accept="image/jpeg,image/jpg,image/png,image/webp"
                  style="display: none"
                />
                <!-- Bouton pour déclencher l'upload -->
                <button
                  type="button"
                  class="btn btn--red btn-upload"
                  id="uploadButton"
                >
                  Je télécharge mon image
                  <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide-icon btn-upload-icon"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" x2="12" y1="3" y2="15"/></svg>
                </button>
                <p class="image-copyright">
                  * Votre image sera validée par Ciné Délices avant publication
                  (respect des droits d'auteur requis).
                </p>
              </div>
              <h3 class="recipe-context-title">Contexte de la recette</h3>
              <textarea
                name="description"
                id="recipe-context"
                class="recipe-context-textarea"
                placeholder="Décrivez la scène du film où cette recette apparaît ou qui vous a inspiré..."
                spellcheck="true"
                lang="fr"
                maxlength="500"
              ></textarea>
            </div>
          </div>

          <!-- Section Ingrédients et Préparation -->
          <h4 class="ingredients-preparation-subtitle">
            3. J'ajoute ma liste d'ingrédients et ma préparation étape par étape
          </h4>
          <div class="ingredients-preparation-container">
            <div class="ingredients-column">
              <div class="overlay"></div>
              <h3>Ingrédients</h3>
              <p class="recipe-field-hint">
                Astuce : un retour à la ligne = un ingrédient.
              </p>
              <textarea
                name="ingredients"
                id="ingredients"
                class="recipe-textarea"
                placeholder="Ex :&#10;200g de farine&#10;3 œufs&#10;100g de sucre"
                spellcheck="true"
                lang="fr"
              ></textarea>
            </div>
            <div class="preparation-column">
              <div class="overlay"></div>
              <h3>Préparation</h3>
              <p class="recipe-field-hint">
                Astuce : un retour à la ligne = une étape.
              </p>
              <textarea
                name="preparation"
                id="preparation"
                class="recipe-textarea"
                placeholder="Ex :&#10;Préchauffer le four à 180°C&#10;Mélanger les ingrédients"
                spellcheck="true"
                lang="fr"
              ></textarea>
            </div>
          </div>

          <!-- Footer du formulaire -->
          <div class="form-validation-footer">
            <p class="validation-note">
              * Votre recette sera soumise à validation par l'équipe Ciné
              Délices avant publication.
            </p>
            <button
              type="submit"
              class="btn btn--red form-validation-btn"
              id="submit-unified-form"
            >
              Je valide ma recette
            </button>
          </div>
        </form>
      </section>
    </main>

    <!-- FOOTER -->
    <%- include("partials/footer") %>

    <!-- Scripts JS globaux -->
    <script src="/js/burger-menu.js" defer></script>
    <!-- effet glassmorphism -->
    <script src="/js/glowy-container.js" defer></script>
    <!-- prévisualisation image upload -->
    <script src="/js/recipe-image-upload.js" defer></script>
    <!-- utils centralisés (doivent être chargés en premier) -->
    <script src="/js/utils/film-image-utils.js" defer></script>
    <script src="/js/utils/url-utils.js" defer></script>
    <script src="/js/utils/text-utils.js" defer></script>
    <!-- autocomplétion film dans formulaire -->
    <script src="/js/movie-autocomplete-form.js" defer></script>
    <!-- gestionnaire formulaire unifié -->
    <script src="/js/unified-form-handler.js" defer></script>
    <!-- gestionnaire bouton "Je passe à la recette" -->
    <script src="/js/film-preview-handler.js" defer></script>
    <!-- validation intelligente TMDB -->
    <script src="/js/tmdb-validator.js" defer></script>
    <!-- pré-remplissage formulaire depuis TMDB -->
    <script src="/js/tmdb-form-prefill.js" defer></script>
    <!-- chargement de l'image pour les films existants -->
    <script src="/js/load-existing-movie-image.js" defer></script>
    <!-- auto-resize des textareas -->
    <script src="/js/add-recipes-movies-textarea.js" defer></script>
  </body>
</html>

<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Ciné Délices — Découvrez tous les films et séries cultes et leurs recettes"
    />
    <title>Recettes du film - Ciné Délices</title>

    <!-- Ressources globales : Google Fonts, FontAwesome, base.css -->
    <%- include("partials/head-resources") %>

    <!-- styles-->
    <link rel="stylesheet" href="/css/reset.css" />
    <link rel="stylesheet" href="/css/recipes-movie.css" />

    <!-- scripts -->
    <script src="/js/filtred-category.js" defer></script>
    <script src="/js/more-views.js" defer></script>
  </head>

  <body>
    <!-- HEADER -->
    <%- include("partials/header") %>

    <!-- MAIN -->
    <main>
      <% const hasAuthorFilter = typeof isAuthorFiltered !== "undefined" ? isAuthorFiltered : false; %>
      <% const authorImg = typeof authorProfileImage !== "undefined" ? authorProfileImage : null; %>
      <% const authorBanner = typeof authorBannerImage !== "undefined" ? authorBannerImage : null; %>
      <% const currentAuthorId = typeof authorId !== "undefined" ? authorId : null; %>
      <% const bannerImage = (hasAuthorFilter && authorBanner) ? authorBanner : (movie ? (movie.bannerPath || movie.picture) : "/images/image-banner-movies.jpg"); %>
      <section class="banner__image__film" aria-hidden="true">
        <div class="banner__image__film__overlay"></div>
        <img
          src="<%= bannerImage %>"
          alt="<%= movie ? `Image du film ${movie.title}` : 'Image de présentation des recettes' %>"
          width="1440"
          height="350"
          decoding="async"
        />
        <% if (hasAuthorFilter && userId && currentAuthorId && String(userId) === String(currentAuthorId)) { %>
          <button class="banner__edit-btn" id="editBannerBtn" type="button" data-user-id="<%= userId %>">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/></svg>
            Modifier ma bannière
          </button>
        <% } %>
      </section>

      <section class="banner <%= movie ? '' : 'banner--recipes' %>" aria-labelledby="movie-title">
        <div class="banner__title" id="movie" data-id="<%= movie ? movie.id : '' %>">
          <% if (movie) { %>
            <p class="banner__meta">Film ou série culte</p>
            <h2 id="movie-title"><%= movie.title %></h2>
            <p class="banner__subtitle">
              <span class="film-year"><%= movie.year %></span>
              <span class="film-separator">•</span>
              <span class="film-genre"><%= movie.genre %></span>
            </p>
            <% if (movie.synopsis) { %>
            <div class="banner__synopsis">
              <p class="banner__synopsis-text" id="synopsis-text"><%= movie.synopsis %></p>
              <button class="banner__synopsis-toggle" id="synopsis-toggle" type="button" aria-expanded="false">
                Lire le synopsis
              </button>
            </div>
            <% } %>
          <% } else { %>
            <p class="banner__meta">Recettes Ciné Délices</p>
            <h2 id="movie-title">
              <%= hasAuthorFilter && authorDisplayName ? `Recettes de ${authorDisplayName}` : "Toutes les recettes" %>
            </h2>
            <p class="banner__subtitle">
              Découvrez toutes les recettes inspirées du cinéma et partagez vos créations.
            </p>
            <% if (hasAuthorFilter) { %>
              <a class="banner__reset-link" href="/recipes-movie">
                Voir toutes les recettes
              </a>
            <% } %>
          <% } %>
        </div>

        <% if (!movie && hasAuthorFilter && authorImg) { %>
          <div class="banner__author-badge">
            <div class="banner__author-badge__avatar">
              <img
                src="<%= authorImg %>"
                alt="Photo de <%= authorDisplayName %>"
                width="48"
                height="48"
                loading="lazy"
              />
            </div>
            <span class="banner__author-badge__name"><%= authorDisplayName %></span>
          </div>
        <% } %>

        <% if (movie) { %>
          <div class="banner__search__films">
            <label for="search" class="search__label">
              Rechercher une recette par catégorie :
            </label>

            <select name="recipes" id="search" class="search__select__film">
              <option value="" selected>Catégories</option>
              <option value="all">Toutes les recettes</option>
              <option value="entrée">Entrée</option>
              <option value="plat">Plat</option>
              <option value="dessert">Dessert</option>
            </select>
          </div>
        <% } %>
      </section>

      <!-- SECTION RECETTES -->
      <section id="recipes">
        <div class="presentation__recipes">
          <div class="presentation__text__recipes">
            <% if (movie) { %>
              <h3>Nos recettes cultes</h3>
              <p>
                Découvrez les recettes inspirées de ce film ou de cette série et
                revivez les saveurs du grand écran
              </p>
            <% } else { %>
              <h3>
                <%= hasAuthorFilter && authorDisplayName ? `Recettes de ${authorDisplayName}` : "Nos recettes cultes" %>
              </h3>
              <p>
                Explorez les recettes inspirées du cinéma et trouvez votre prochaine inspiration.
              </p>
            <% } %>
          </div>

          <div class="presentation__button__recipes">
            <a
              class="btn btn--gold"
              href="<%= movie ? `/add-recipes-movies/${movie.id}` : '/add-recipes-movies/' %>"
            >
              <span class="btn__shimmer"></span>
              <svg class="icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
              <span class="btn__text">Ajouter ma recette</span>
            </a>
          </div>
        </div>

        <div id="recipes__list" class="articles only__display__9__recipes">
          <% if (!recipes || recipes.length === 0) { %>
          <p class="no-recipes">
            Aucune recette n'est disponible pour ce film.
          </p>
          <% } else { %> <% recipes.forEach(recipe => { %>
          <%
            const displayRating = recipe.avgRating || recipe.quote || '0';
            // L'auteur est toujours présent (id_user NOT NULL + jointure User côté backend)
            const authorName = recipe.contributor.pseudo;
            const authorAvatar = recipe.contributor.picture || null;
            const isValidated = recipe.status === true;
            const recipeDescription = recipe.description || recipe.anecdote || 'Découvrez cette délicieuse recette inspirée du cinéma...';
            const truncatedDescription = recipeDescription.length > 80 ? recipeDescription.substring(0, 80) + '...' : recipeDescription;
          %>
          <article class="recipe-card">
            <!-- Image avec overlay -->
            <div class="recipe-card__image">
              <img
                src="<%= recipe.picture || '/images/image-default-recipe-1.jpg' %>"
                alt="<%= recipe.name %>"
                loading="lazy"
                width="640"
                height="360"
                decoding="async"
              />
              <div class="recipe-card__image-overlay">
                <% if (isValidated) { %>
                <span class="badge badge--velvet">
                  <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>
                  Validée
                </span>
                <% } else { %>
                <span class="badge badge--silver">
                  <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                  En attente
                </span>
                <% } %>
                <button
                  class="btn-favorite"
                  data-entity-id="<%= recipe.id %>"
                  data-entity-type="recipe"
                  data-is-favorite="<%= recipe.isFavorite || false %>"
                  aria-label="<%= recipe.isFavorite ? 'Retirer des favoris' : 'Ajouter aux favoris' %>"
                  title="<%= typeof userId === 'undefined' || !userId ? 'Connectez-vous pour ajouter aux favoris' : (recipe.isFavorite ? 'Retirer des favoris' : 'Ajouter aux favoris') %>"
                >
                  <svg class="icon" viewBox="0 0 24 24" fill="<%= recipe.isFavorite ? 'currentColor' : 'none' %>" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/></svg>
                </button>
              </div>
            </div>

            <!-- Titre -->
            <h3 class="recipe-card__title"><%= recipe.name %></h3>

            <!-- Tags meta -->
            <div class="recipe-card__meta">
              <span class="tag tag--red"><%= recipe.category %></span>
              <span class="tag tag--silver">
                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>
                <%= recipe.duration || recipe.time %> min
              </span>
              <span class="tag tag--velvet"><%= recipe.difficulty %></span>
            </div>

            <!-- Notation -->
            <div class="rating rating--sm" data-entity-id="<%= recipe.id %>" data-entity-type="recipe" data-average="<%= displayRating %>">
              <% for (let i = 1; i <= 5; i++) { %>
              <span class="rating__star <%= i <= Math.round(parseFloat(displayRating)) ? 'rating__star--filled' : '' %>">
                <svg class="icon" viewBox="0 0 24 24"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
              </span>
              <% } %>
              <span class="rating__value"><%= displayRating %></span>
            </div>

            <!-- Description (2 lignes) -->
            <p class="recipe-card__description"><%= truncatedDescription %></p>

            <!-- Footer : Auteur + Bouton Voir -->
            <div class="recipe-card__footer">
              <a href="/recipes-movie?author=<%= encodeURIComponent(authorName.toLowerCase()) %>" class="author">
                <span class="author__avatar">
                  <% if (authorAvatar) { %>
                  <img src="<%= authorAvatar %>" alt="<%= authorName %>" />
                  <% } else { %>
                  <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21a1 1 0 0 0 1-1v-5.35c0-.457.316-.844.727-1.041a4 4 0 0 0-2.134-7.589 5 5 0 0 0-9.186 0 4 4 0 0 0-2.134 7.588c.411.198.727.585.727 1.041V20a1 1 0 0 0 1 1Z"/><path d="M6 17h12"/></svg>
                  <% } %>
                </span>
                <span class="author__name"><%= authorName %></span>
              </a>
              <a class="btn btn--primary btn--sm" href="/recipes-movie/details/<%= recipe.id %>">
                <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"/><circle cx="12" cy="12" r="3"/></svg>
                Voir
              </a>
            </div>
          </article>
          <% }) %> <% } %>
        </div>

        <% if (recipes && recipes.length > 9) { %>
        <div class="center-button">
          <button
            id="see__all__recipes__button"
            class="btn btn--red"
            aria-label="Voir plus de recettes"
          >
            <span class="btn__shimmer"></span>
            <svg class="icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 13.87A4 4 0 0 1 7.41 6a5.11 5.11 0 0 1 1.05-1.54 5 5 0 0 1 7.08 0A5.11 5.11 0 0 1 16.59 6 4 4 0 0 1 18 13.87V21H6Z"/><line x1="6" x2="18" y1="17" y2="17"/></svg>
            <span class="btn__text">Voir plus</span>
          </button>
        </div>
        <% } %>
      </section>
    </main>

    <!-- FOOTER -->
    <%- include("partials/footer") %>

    <!-- Scripts JS globaux -->
    <script src="/js/burger-menu.js" defer></script>

    <!-- Toggle synopsis "Lire plus / Lire moins" -->
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        const toggle = document.getElementById("synopsis-toggle");
        const text = document.getElementById("synopsis-text");
        if (!toggle || !text) return;

        toggle.addEventListener("click", function () {
          const isExpanded = text.classList.toggle("banner__synopsis-text--expanded");
          toggle.textContent = isExpanded ? "Lire moins" : "Lire le synopsis";
          toggle.setAttribute("aria-expanded", isExpanded);
        });
      });
    </script>

    <!-- Configuration pour les favoris -->
    <script>
      window.CINEDELICES = {
        isLoggedIn: <%= typeof userId !== 'undefined' && userId !== null %>,
        userId: <%= typeof userId !== 'undefined' && userId !== null ? userId : 'null' %>
      };
    </script>
    <script src="/js/favorites.js" defer></script>

    <% if (hasAuthorFilter && userId && currentAuthorId && String(userId) === String(currentAuthorId)) { %>
    <!-- ============================================================
         MODALE BANNIÈRE AVEC CROPPER
         ─────────────────────────────
         Le cropper permet à l'utilisateur de :
         1. Choisir une image (bouton ou clic sur le placeholder)
         2. Zoomer avec le slider ou la molette de souris
         3. Déplacer l'image en glissant avec la souris/doigt
         4. Voir un aperçu en temps réel dans le canvas (ratio 1408:350)
         5. Enregistrer → le canvas génère un Blob JPEG envoyé au serveur
         ============================================================ -->
    <div class="banner-modal" id="bannerModal" aria-hidden="true">
      <div class="banner-modal__overlay"></div>
      <div class="banner-modal__dialog">
        <button type="button" class="banner-modal__close" id="closeBannerModal" aria-label="Fermer">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
        </button>
        <h3 class="banner-modal__title">Modifier ma bannière</h3>
        <p class="banner-modal__hint">Format final : 1408 × 350 pixels — Zoomez et déplacez l'image pour cadrer</p>

        <!-- Zone de recadrage : le canvas affiche l'aperçu en temps réel -->
        <div class="banner-cropper" id="bannerCropperContainer">
          <!-- Canvas de recadrage (ratio fixe 1408:350 via CSS) -->
          <canvas class="banner-cropper__canvas" id="bannerCropperCanvas"></canvas>
          <!-- Placeholder visible quand aucune image n'est chargée -->
          <div class="banner-cropper__placeholder" id="bannerPlaceholder">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="3" rx="2" ry="2"/><circle cx="9" cy="9" r="2"/><path d="m21 15-3.086-3.086a2 2 0 0 0-2.828 0L6 21"/></svg>
            <span>Cliquez pour choisir une image</span>
          </div>
        </div>

        <!-- Contrôle de zoom (visible uniquement quand une image est chargée) -->
        <div class="banner-cropper__zoom" id="bannerZoomControl">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/><path d="M8 11h6"/></svg>
          <input type="range" id="bannerZoomRange" min="0" max="100" value="0" />
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.3-4.3"/><path d="M11 8v6"/><path d="M8 11h6"/></svg>
        </div>

        <!-- Input file caché (déclenché par le bouton "Choisir une image") -->
        <input type="file" id="bannerInput" accept="image/jpeg,image/png,image/webp" hidden />

        <div class="banner-modal__actions">
          <label for="bannerInput" class="btn btn--gold btn--sm">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="17 8 12 3 7 8"/><line x1="12" x2="12" y1="3" y2="15"/></svg>
            Choisir une image
          </label>
          <button type="button" class="btn btn--red btn--sm" id="resetBannerBtn">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"/><path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"/></svg>
            Bannière par défaut
          </button>
        </div>
        <div class="banner-modal__footer">
          <button type="button" class="btn btn--gold" id="saveBannerBtn" disabled>Enregistrer</button>
          <button type="button" class="btn btn--outline" id="cancelBannerModal">Annuler</button>
        </div>
      </div>
    </div>
    <!-- Le cropper (classe BannerCropper) doit être chargé AVANT banner-upload.js qui l'instancie -->
    <script src="/js/banner-cropper.js" defer></script>
    <script src="/js/banner-upload.js" defer></script>
    <% } %>
  </body>
</html>
